---

- name: bkp_tcl
  hosts: tests

  tasks:
  - name: backup themecloud script
    local_action: ansible.builtin.command /home/fabrice/backup-script/checkport.sh
    register: shell_result
  - debug: 
      var: shell_result.stdout_lines
